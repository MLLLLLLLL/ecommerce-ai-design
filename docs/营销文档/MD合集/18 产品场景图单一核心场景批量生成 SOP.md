# 18 产品场景图单一核心场景批量生成 SOP

[7月3日场景图生成\.mp4](图片和附件/7月3日场景图生成.mp4)

## 适用场景

这份 SOP 用于生成电商产品场景图，核心逻辑是：`1 个核心场景 + 统一风格`。

适合：

1. 商家要给产品生成一套视觉统一的场景主图。

2. 工具里需要提供“类目高频场景”给用户选择。

3. 用户没有对标图，只知道产品类目，希望快速选一个最常用、最合理的场景。

4. 同一个产品或同一批产品要复用同一个场景风格，避免主图组视觉混乱。

不适合：

1. 一张图里混多个场景。

2. 每张图都随机换一个背景。

3. 平台严格白底首图。

4. 详情页信息海报、参数图、促销图。

5. 真实手机买家秀；这类需求优先使用 \[\[上传产品批量生成相同场景真实手机买家秀 SOP\]\]。

## 核心原则

场景图大多应该选择 `1 个核心场景 + 统一风格`。

例如：

1. 卖户外帐篷，固定“露营草地场景”。

2. 卖厨房用品，固定“现代厨房场景”。

3. 卖女装，固定“通勤办公室 / 周末咖啡馆 / 户外公园”中的一个。

4. 卖美妆护肤，固定“梳妆台 / 浴室洗手台 / 晨间护肤桌面”中的一个。

这样整套主图视觉统一，用户不会觉得每张图来自不同店铺、不同品牌或不同拍摄方案。

硬规则：

1. 同一张场景图里不要混多个场景。

2. 同一组场景图默认复用同一个核心场景。

3. 批量生成时，可以变化产品角度、镜头远近、局部特写、道具位置和构图，但不能切换到另一个空间。

4. 如果确实需要多场景，应该把不同场景分散在不同主图、不同页面或不同任务里，每张图仍然只表达一个场景。

5. 场景必须服务产品真实使用关系，不能暗示产品不存在的功能、尺寸、配件、效果或使用方式。



## 类目高频场景预设

按类目预设，默认单选。

如果类目不在表内，按下面公式自动生成场景选项：

```Plain Text
类目高频场景 = 产品最常被使用的空间 + 用户最容易理解产品价值的位置 + 不会误导功能的真实环境
```

## 用户输入区模板

```Plain Text
产品名称：
产品类目：
目标平台：
输出比例：默认 1:1，可选 3:4 / 4:5 / 16:9
生成数量：
固定核心场景：从类目高频场景里选择 1 个
统一风格：自然生活方式 / 高级商业摄影 / 小红书种草 / 独立站品牌感 / 内容电商封面感
是否允许人物：不允许 / 允许手部或身体局部 / 允许无脸模特 / 允许普通商业模特
是否允许图中文字：默认不允许
禁止出现：
其他要求：
```

## 直接复制用的总控提示词

把产品图上传给 Gemini / 豆包 / 其他多模态 AI，然后复制下面这段。

你是一名资深电商产品场景图导演、商业摄影策划师和 AI 生图提示词专家。

我会上传一张或多张产品图，并填写产品类目、目标平台、生成数量和固定核心场景。你的任务不是给每张图随机换不同场景，而是围绕用户选择的 1 个核心场景，生成一组视觉统一、风格一致、可以直接用于电商主图组或品牌素材的产品场景图生图提示词。

【输入参数】
产品名称：
产品类目：
目标平台：
输出比例：1:1
生成数量：
固定核心场景：例如现代厨房台面 / 露营草地 / 通勤办公室 / 周末咖啡馆 / 户外公园
统一风格：自然生活方式 / 高级商业摄影 / 小红书种草 / 独立站品牌感 / 内容电商封面感
是否允许人物：不允许 / 允许手部或身体局部 / 允许无脸模特 / 允许普通商业模特
是否允许图中文字：默认不允许
禁止出现：
其他要求：

【核心任务】
请根据上传产品图和用户选择的【固定核心场景】，按【生成数量】输出对应数量的中文产品场景图生图提示词。

整组图必须复用同一个核心场景和同一套视觉风格。可以变化产品角度、镜头远近、构图、道具位置、前后景、局部特写和使用状态，但不得切换到其他场景。

如果用户没有填写固定核心场景，请根据产品类目自动推荐 3 到 6 个高频场景，并要求用户先选择 1 个；不要直接生成多场景混合结果。

如果用户要求多场景，请把不同场景拆成不同主图或不同页面，每张图仍然只表达一个场景，不能在同一张图里混合多个场景。

【后台静默执行，不要输出分析过程】
1\. 识别上传产品的真实品类、外观、结构、颜色、材质、包装、品牌露出、标签、尺寸感和可见细节。
2\. 提炼一段“产品外观锁定描述”，后续每条生图提示词都必须复用。
3\. 判断固定核心场景是否与产品用途匹配；如果明显不匹配，保守提醒并改成更合理的同类场景。
4\. 把固定核心场景扩写成“场景基准描述”，包括空间类型、台面/地面/墙面材质、背景层次、道具体系、光线方向、色温、镜头语言和统一风格。
5\. 按【生成数量】规划同一场景内不同构图，不要把图片规划成多个不同场景。
6\. 确保每张图都服务产品展示和购买理解，而不是只换背景做装饰。

【产品锁定规则】
1\. 必须以上传产品图作为唯一产品外观依据。
2\. 严格保持产品真实品类、轮廓、比例、结构、颜色、材质、包装、品牌露出、标签和所有可见细节不变。
3\. 不得把产品改成别的款式、别的颜色、别的包装、别的容量、别的型号。
4\. 不得新增上传产品图中没有的配件、功能、认证、屏幕内容、图案、Logo 或品牌信息。
5\. 看不清的信息只能保守处理，不能脑补参数、功效、材质等级或使用效果。

【单一核心场景规则】
1\. 一组产品场景图只允许使用 1 个固定核心场景。
2\. 同一张场景图里不得混入多个空间或多个使用环境。
3\. 固定核心场景必须具体，例如“现代厨房灰色石英石台面”“露营草地木质折叠桌”“通勤办公室白色办公桌”“周末咖啡馆木质圆桌”“户外公园长椅旁”。
4\. 场景中的背景、主光方向、色温、道具体系和视觉风格必须保持统一。
5\. 道具可以轻微变化位置，但必须属于同一场景体系，不要每张图换一批无关道具。
6\. 多张图之间的差异只能来自同一场景内部，例如产品角度、镜头远近、局部特写、开箱状态、手部动作、前景遮挡、道具轻微移动、构图变化。
7\. 禁止一张厨房、一张户外、一张办公室、一张咖啡馆地跳场景。

【场景合理性规则】
1\. 场景必须与产品真实用途、目标用户和电商展示目标匹配。
2\. 道具只用于营造真实空间、比例感和氛围，不得暗示产品没有的功能。
3\. 不要生成豪华摄影棚、超现实场景、夸张大片、复杂烟雾、强烈光束、过度电影感，除非用户明确要求且不误导产品。
4\. 产品必须是第一视觉中心，不能被人物、道具、背景或装饰抢走。
5\. 场景图默认不生成大段文字、参数表、促销标签、价格贴纸、水印、平台 Logo 和乱码。

【人物规则】
1\. 如果选择“不允许人物”，全套图不出现人物、手、脚、身体局部和人脸。
2\. 如果选择“允许手部或身体局部”，只允许手部、手臂、脚部、肩部以下、下巴以下裁切、背影或局部使用动作，不出现可识别人脸。
3\. 如果选择“允许无脸模特”，必须写入 no visible face, face fully out of frame, no eyes, no nose, no mouth, no identifiable face。
4\. 如果选择“允许普通商业模特”，只能在平台合规、产品需要人物帮助理解穿戴/使用关系时出现，不能像明星、网红或真人复刻。

【输出格式】
只输出最终可复制的生图提示词，不输出分析、解释和教程。

先输出：
产品外观锁定描述：
【根据上传产品图写一段产品外观锁定描述】

固定场景基准描述：
【把用户选择的 1 个核心场景扩写成统一场景基准】

然后按下面格式输出到【生成数量】对应的最后一条：

场景图一：【同一核心场景下的构图主题】
提示词：
以上传产品图作为唯一产品外观参考，严格保持产品真实品类、轮廓、比例、结构、颜色、材质、包装、品牌露出、标签和所有可见细节不变。复用产品外观锁定描述：【写入产品外观锁定描述】。生成一张【固定核心场景】产品场景图，复用固定场景基准描述：【写入固定场景基准描述】。写清产品在画面中的位置、角度、占比、接触阴影、前中后景、镜头视角、光线方向、道具关系、人物规则、图中文字规则和负面约束。产品是第一视觉中心，场景服务产品展示，不改变产品外观，不新增产品图中没有的配件、功能、规格或品牌信息。high\-end commercial product photography, realistic scene, unified style, natural lighting, premium composition, product accurate, hyper\-detailed, 8k, \-\-ar 1:1

场景图二：【同一核心场景下的另一个构图主题】
提示词：
以上传产品图作为唯一产品外观参考，严格保持产品真实品类、轮廓、比例、结构、颜色、材质、包装、品牌露出、标签和所有可见细节不变。复用同一个固定核心场景和同一套视觉风格，只改变构图、镜头远近、产品角度或局部展示方式，不切换到其他场景。按场景图一的完整标准继续写清产品位置、场景基准、光影、道具、人物规则、文字规则和负面约束。

后续场景图继续按同样格式输出，直到达到【生成数量】。

## 多场景需求处理

如果用户确实要多场景，不要让 AI 在同一张图里混场景，而是拆成不同图片任务。

正确做法：

1. 场景图一：现代厨房台面。

2. 场景图二：餐桌早餐场景。

3. 场景图三：办公室零食角。

错误做法：

1. 一张图里同时出现厨房、办公室、户外、公园。

2. 同一组图每张随机换背景，没有统一风格。

3. 为了展示“多场景适用”，把产品复制成多个小图拼贴在同一张图里。

多场景返修提示词：

```Plain Text
请不要把多个场景混在同一张产品场景图里。请把每个场景拆成独立主图，每张图只保留 1 个核心场景。当前这一张只保留【填写场景】，删除其他场景元素，保持产品外观与上传图一致。
```

## 批量生成操作

### 同一个产品生成统一场景图组

1. 上传同一组产品图。

2. 选择 1 个固定核心场景。

3. 用总控提示词输出多条生图提示词。

4. 每条提示词单独生成一张图。

5. 检查每条提示词是否都复用了同一个场景基准描述。

6. 翻车时只返修单张，不要整组重做。

### 多个产品复用同一场景

如果多个产品要统一品牌视觉，可以使用同一个固定核心场景和同一套提示词结构。

通用提示词：

```Plain Text
以上传产品图作为唯一产品外观依据，严格保持产品真实品类、轮廓、比例、结构、颜色、材质、包装、品牌露出、标签和所有可见细节不变。为该产品生成一张统一风格的产品场景图，固定核心场景为：【填写一个具体场景，例如现代厨房灰色石英石台面 / 露营草地木质折叠桌 / 通勤办公室白色办公桌】。整组产品都复用同一个场景空间、同一背景材质、同一主光方向、同一色温、同一道具体系和同一商业摄影风格。产品自然出现在画面中，占画面约 35%-55%，有真实接触阴影、合理透视和真实尺度。道具只用于营造空间层次和真实使用氛围，不遮挡产品，不暗示不存在的功能。不要生成无关文字、乱码、水印、平台 Logo、价格标签、促销贴纸、参数卡片和详情页排版。不要改变产品外观，不要新增产品图中没有的配件、功能、规格、认证、品牌信息或包装文字。high-end commercial product photography, realistic scene, unified style, natural lighting, premium composition, product accurate, hyper-detailed, 8k, --ar 1:1
```

## 验收清单

生成后逐张检查：

1. 是否只使用了 1 个核心场景。

2. 同一组图的背景、光线、色温、道具体系和视觉风格是否统一。

3. 多张图之间是否只是构图、角度、远近、产品状态和局部展示不同，而不是场景乱跳。

4. 产品是否与上传图一致，没有变色、变形、换包装、改结构。

5. 产品是否是第一视觉中心，没有被道具、人物或背景抢走。

6. 场景是否符合产品真实用途，没有暗示不存在的功能。

7. 是否没有无关文字、乱码、水印、价格、促销标签和平台 Logo。

8. 如果不允许人物，是否完全没有人物、手脚和身体局部。

9. 如果允许无脸模特，是否没有眼睛、鼻子、嘴、侧脸、半张脸和可识别人脸。

## 常用返修提示词

### 场景混乱

```Plain Text
请把这组产品场景图统一回 1 个固定核心场景：【填写场景】。整组图必须保持同一空间、同一背景材质、同一光线方向、同一色温、同一道具体系和同一商业摄影风格。只允许变化产品角度、镜头远近、局部特写、道具轻微位置和构图，不要切换到其他场景。
```

### 一张图里混了多个场景

```Plain Text
请删除同一张图里的其他场景，只保留一个核心场景：【填写场景】。不要在一张图里同时出现厨房、办公室、户外、咖啡馆等多个空间。产品外观保持与上传图一致，场景服务产品展示。
```

### 场景不符合类目

```Plain Text
请重新选择更符合该产品真实用途的核心场景。场景应来自产品最常被使用的空间、用户最容易理解产品价值的位置，并且不能误导产品功能。保持产品外观与上传图一致。
```

### 产品被场景抢走

```Plain Text
请减少背景和道具复杂度，让产品重新成为第一视觉中心。产品占画面约 35%-55%，主体清晰完整，有真实接触阴影。道具只保留少量与产品品类相关的辅助元素，不要遮挡产品或抢走注意力。
```

### 视觉风格不统一

```Plain Text
请统一整组图的风格，保持同一场景、同一光线方向、同一色温、同一背景材质、同一道具体系和同一商业摄影质感。不要让每张图像来自不同品牌或不同拍摄方案。
```

## 相关页面

- \[\[反推对标场景并批量生成\]\]

- \[\[07 全平台品类主图提示词 SOP（可选模特与人脸规则）\]\]

- \[\[上传产品批量生成相同场景真实手机买家秀 SOP\]\]

