## [00:40:00] 深度评审核心问题修复

- **修改说明**：根据 `PROJECT_DEEP_REVIEW_REPORT_2026-08-15.md` 修复模型地址与密钥复用链、服务端 SSRF 出站校验、生成图片下载限制、资产与文件归属校验、私有缓存、文本导出对象存储、Worker 构建期副作用与租约 fencing、异步任务原子创建、营销轮询重试、V3 批量操作空回调，以及工作流条件分支和图像处理伪实现；按要求未实现 P0-01 登录/认证系统。
- **受影响文件**：`src/lib/security/safe-url.ts`、模型/搜索/AI API、资产/文件 API、营销上传与异步 Worker、营销导出与存储、工作流节点与引擎、营销前端轮询与 V3 向导、`docker-compose.yml`、`package.json`。

## [00:43:00] 补齐资源归属边界

- **修改说明**：为画布项目、工作流、文件夹及其动态资源路由补充当前用户过滤、父文件夹归属校验和不存在资源的 404 响应，降低 IDOR 风险。
- **受影响文件**：`src/app/api/canvas-projects/[id]/route.ts`、`src/app/api/workflows/[id]/route.ts`、`src/app/api/folders/[id]/route.ts`。

## [00:45:00] 适配器兼容构建

- **修改说明**：将可能被浏览器工作流包引用的 AI 适配器改用无 Node 依赖的轻量 URL 防护，保留服务端严格 DNS 私网校验，避免 Next.js 客户端构建引入 `node:dns`。
- **受影响文件**：`src/lib/security/safe-fetch.ts`、`src/lib/ai/adapters/openai.ts`、`src/lib/ai/adapters/alibaba.ts`、`src/lib/ai/adapters/relay.ts`。

## [01:28:41] 收敛营销助手2主流程

- **修改说明**：按“主图详情页全自动生成”为唯一可新建流程清理营销助手2：删除旧版通用运行器、步骤栏、模型选择器及四个独立工作流定义和输入 Schema；保留主流程五步能力、任务 API、异步执行、资产版本与历史任务查询。历史 V2 任务保留只读查看，禁止继续编辑、执行、重试、暂停、取消或返修。移除旧工作流导入、独立提示词/参考图/资产选择输入和无后端效果的“批量提交”开关；将 JSON、Markdown、提示词包、质检报告、资产清单导出入口补回 V3 完成页。同步更新单元测试与 E2E 测试。
- **受影响文件**：`src/app/(dashboard)/marketing2/[workflowKey]/page.tsx`、`src/components/marketing2/WorkflowRunner.tsx`、`src/components/marketing2/WorkflowStepper.tsx`、`src/components/marketing2/StepModelSelector.tsx`、`src/components/marketing2/WorkflowCard.tsx`、`src/components/marketing2/hooks/use-marketing2-run.ts`、`src/components/marketing2/steps/MaterialStep.tsx`、`src/components/marketing2/steps/BatchGenerationStep.tsx`、`src/components/marketing2-v3/WorkflowWizard.tsx`、`src/lib/marketing2/workflow-registry.ts`、`src/lib/marketing2/schemas.ts`、`src/lib/marketing2/run-service.ts`、`src/lib/marketing2/step-actions.ts`、`src/lib/marketing2/__tests__/schemas.test.ts`、`src/lib/marketing2/__tests__/workflow-registry.test.ts`、`tests/e2e/marketing2.spec.ts`、`tests/e2e/marketing2-api.spec.ts`、`docs/2026-08-16.md`。

## [01:42:00] 清理主流程 lint 噪音

- **修改说明**：移除本次主流程收敛后不再使用的 UI 导入和执行上下文变量，保持营销助手2相关文件无新增未使用项。
- **受影响文件**：`src/components/marketing2/steps/MaterialStep.tsx`、`src/lib/marketing2/step-actions.ts`、`docs/2026-08-16.md`。

## [01:50:07] 展示全部可删除草稿

- **修改说明**：营销助手2“最近任务”改为通过游标分页加载全部草稿生命周期任务，不再固定只取 5 条；草稿处于待确认、执行中或部分失败等阶段时仍显示删除入口，服务端同步允许这些草稿阶段直接删除，并保留 force 参数用于历史任务清理；任务列表游标增加创建时间与任务 ID 组合，避免同一时间创建的任务被漏列。
- **受影响文件**：`src/app/(dashboard)/marketing2/page.tsx`、`src/components/marketing2/hooks/use-marketing2-run.ts`、`src/lib/marketing2/run-service.ts`、`src/app/api/marketing2/runs/[id]/route.ts`、`docs/2026-08-16.md`。

## [01:58:18] 接入资源库多选操作

- **修改说明**：资源库增加“多选”入口和当前页全选能力，选中资源后支持批量删除、添加到画布和添加到工作流；多选操作不提供批量查看，单资源查看入口保持原样。资源库项目选择器支持携带多张图片，工作流桥接新增图片队列并在目标工作流中批量创建图片输入节点。
- **受影响文件**：`src/app/(dashboard)/assets/page.tsx`、`src/components/assets/BatchOperations.tsx`、`src/stores/workflowBridge.ts`、`src/app/(dashboard)/workflow/[id]/page.tsx`、`docs/2026-08-16.md`。

## [02:21:26] 支持点击图片选中

- **修改说明**：多选模式下将资源卡片的图片展示区域设为选中/取消选中的切换区域，并补充键盘 Enter/空格操作和选中态边框；复选框点击会阻止事件冒泡，避免一次点击发生两次切换。
- **受影响文件**：`src/app/(dashboard)/assets/page.tsx`、`docs/2026-08-16.md`。
