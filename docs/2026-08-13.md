# 2026-08-13 开发日志

## [00:15:00] Phase 0 工程门禁与契约冻结

- **修改说明**：按 V3 设计文档执行 Phase 0。引入 Vitest（含 globals 兼容存量 Jest 风格测试、自动加载 .env）、Playwright（三视口营销页冒烟）；冻结营销模块 V3 契约（任务模块枚举、状态机、执行步骤、结构化事实、API 响应规范、错误码）；冻结 Zod Schema（图片 1-5 张边界、UUID 校验、strict 校验）；实现任务状态聚合纯函数 `aggregateTaskStatus`；定义统一文本补全客户端接口与错误分类 `TextCompletionClient`/`classifyHttpStatus`；实现五类场景模型调用 Mock（成功/超时/429/5xx/无效JSON）。新增 39 个单元测试与 9 个冒烟用例，全部通过；tsc/build/prisma validate 通过，新增文件 lint 零错误。
- **受影响文件**：
  - `package.json`（新增 test/test:watch/test:e2e 脚本，devDependencies 增加 vitest、@playwright/test）
  - `vitest.config.ts`（新增）
  - `playwright.config.ts`（新增）
  - `tests/e2e/marketing.spec.ts`（新增）
  - `src/types/marketing-contract.ts`（新增）
  - `src/lib/marketing/schemas.ts`（新增）
  - `src/lib/marketing/task-status.ts`（新增）
  - `src/lib/marketing/__tests__/schemas.test.ts`（新增）
  - `src/lib/marketing/__tests__/task-status.test.ts`（新增）
  - `src/lib/ai/text-completion-client.ts`（新增）
  - `src/lib/ai/testing/mock-model-client.ts`（新增）
  - `src/lib/ai/testing/__tests__/mock-model-client.test.ts`（新增）

## [00:40:00] Phase 1 模型与图片基础能力

- **修改说明**：按 V3 文档执行 Phase 1。（1）新增统一文本补全客户端 `HttpTextCompletionClient`：URL 拼接、鉴权头、response_format 兼容降级重试（不占重试配额）、上游错误解析与脱敏、超时 AbortSignal、可重试错误分类（仅超时/429/5xx，指数退避最多 2 次）、内容提取（string/array/object）与 requestId 日志。（2）新增 `json-response.ts`：JSON 提取（围栏/解释文字/尾逗号/双层解码/包装键）与 completeJSON（Zod Schema 校验，schema_mismatch 不可盲目重试，可选模型修复一次）。（3）MultimodalAdapter 重构为注入 TextCompletionClient，删除内部重复的 callOpenAICompatible 与 JSON 解析实现。（4）新增三类模型实测（connection/jsonMode/vision，内置红色测试卡 base64），实测结果保存到 ModelConfig 新字段 lastTestedAt/testStatus/testedCapabilities/testError，Prisma 迁移 add_model_test_fields；设置页测试按钮升级为实测能力并展示测试状态与脱敏错误摘要。（5）新增 POST /api/marketing/upload 营销图片上传：FormData 多文件、单张 10MB、1-5 张、JPEG/PNG/WebP、MIME 与文件头（sharp metadata）双校验、随机 UUID 文件名、相对路径保存在 USER_DATA_PATH/marketing 下、返回 /api/files URL 与 UPLOAD_INVALID 错误码；实测伪装文件返回 400。（6）新增 35 个单元测试，累计 86 个全部通过；tsc/build/lint 通过。
- **受影响文件**：
  - `prisma/schema.prisma`（ModelConfig 增加实测字段）
  - `prisma/migrations/*add_model_test_fields*`（新增迁移）
  - `src/lib/ai/text-completion-client.ts`（新增 schema_mismatch 错误类型）
  - `src/lib/ai/http-text-completion-client.ts`（新增）
  - `src/lib/ai/json-response.ts`（新增）
  - `src/lib/ai/model-tester.ts`（新增）
  - `src/lib/ai/testing/mock-model-client.ts`（invalid-json 场景改为返回无效文本）
  - `src/lib/ai/testing/__tests__/http-text-completion-client.test.ts`（新增）
  - `src/lib/ai/testing/__tests__/json-response.test.ts`（新增）
  - `src/lib/ai/testing/__tests__/model-tester.test.ts`（新增）
  - `src/lib/marketing/adapters/MultimodalAdapter.ts`（重构使用统一客户端）
  - `src/app/api/marketing/upload/route.ts`（新增）
  - `src/app/api/model-configs/[id]/test/route.ts`（改造为三类实测并保存结果）
  - `src/lib/model-configs.ts`（toModelConfigSummary 带出实测字段）
  - `src/types/model-config.ts`（ModelConfigSummary/TestedCapabilities）
  - `src/app/(dashboard)/settings/page.tsx`（实测按钮与状态展示）

## [00:50:00] Phase 2 文案工作台纵向闭环

- **修改说明**：按 V3 文档执行 Phase 2。（1）Prisma 增量扩展 MarketingTask：module/input/result/selectedOutputs/isFavorite/schemaVersion（全部可空或有默认值，旧接口行为不变），迁移 add_marketing_task_v3_fields；同时修正上一迁移时间戳顺序问题（rename 为 20260813003000 并同步 _prisma_migrations 记录）。（2）新增营销任务服务 task-service：模型解析+能力校验+实测预检（未实测/配置变更 → MODEL_TEST_REQUIRED）、分析串行+下游三类内容 Promise.allSettled 并发（进程内按用户+模型限流 3，p-queue）、aggregateTaskStatus 聚合后一次写回（避免丢失更新）、稳定错误码映射（VALIDATION_ERROR/MODEL_NOT_FOUND/MODEL_CAPABILITY_MISSING/MODEL_TEST_REQUIRED/UPSTREAM_RATE_LIMITED/UPSTREAM_FAILED/OUTPUT_INVALID/TASK_NOT_FOUND/EXPORT_FAILED）、结构化事实（用户输入 confirmed、视觉分析 pending、占位符进入 pendingFacts）。（3）新增 API：POST/GET /api/marketing/tasks（游标分页）、GET/PATCH /api/marketing/tasks/[id]（详情/收藏）、POST /api/marketing/tasks/[id]/export（JSON/Markdown 落盘并创建 Asset 关联 marketingTaskId）。（4）新增五 Tab 工作台（文案可用，其余显示阶段状态）：桌面左右分栏/移动上下、Tab 切换不清空（forceMount）、sessionStorage 草稿（useSyncExternalStore 同步，不含图片 URL 与密钥）、语言 30 种集中配置+平台默认语言联动（手动选择后 languageOverridden）、图片先上传后引用、生成中按钮禁用。（5）结果三级视图：本轮作品（状态徽章/复制/导出/待确认事实）、窗口历史（sessionStorage taskId 回查）、全部作品（状态/收藏/搜索筛选+加载更多+收藏切换+导出）。（6）旧向导保留 ?mode=legacy 入口可回退。新增 11 个单元测试与 6 个 E2E 用例，累计单元 97、E2E 15 全部通过；tsc/build/prisma validate 通过，修改文件 lint 零错误；实测校验 400/404/列表接口均正常。
- **受影响文件**：
  - `prisma/schema.prisma`（MarketingTask 增量字段）
  - `prisma/migrations/20260812162556_add_marketing_task_v3_fields/`（新增）
  - `prisma/migrations/20260813003000_add_model_test_fields/`（时间戳修正）
  - `src/lib/marketing/task-service.ts`（新增）
  - `src/lib/marketing/concurrency.ts`（新增）
  - `src/lib/marketing/languages.ts`（新增，30 种语言）
  - `src/lib/marketing/adapters/MultimodalAdapter.ts`（透传 TextCompletionError）
  - `src/app/api/marketing/tasks/route.ts`（新增）
  - `src/app/api/marketing/tasks/[id]/route.ts`（新增）
  - `src/app/api/marketing/tasks/[id]/export/route.ts`（新增）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（新增）
  - `src/components/marketing/workspace/CopywritingTab.tsx`（新增）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（新增）
  - `src/components/marketing/workspace/PhasePlaceholderTab.tsx`（新增）
  - `src/components/marketing/workspace/platform-list.ts`（新增）
  - `src/app/(dashboard)/marketing/page.tsx`（新旧工作台切换）
  - `src/types/marketing-contract.ts`（响应 data 类型）
  - `src/lib/marketing/__tests__/task-service.test.ts`（新增）
  - `tests/e2e/marketing.spec.ts`（新增 Tab 切换与旧向导入口用例）
  - `.gitignore`（test-results/playwright-report）

## [01:10:00] Phase 3 语言与翻译

- **修改说明**：按 V3 文档执行 Phase 3。（1）翻译引擎 TranslateEngine：prompt 强制保留段落/换行/列表符号、不翻译品牌名与专有名词、JSON 输出经 completeJSON+Zod 校验（结构不符 → schema_mismatch → OUTPUT_INVALID）。（2）语言代码集中校验 language-codes.ts，源语言支持 auto，目标语言必须来自 30 种集中配置，防 prompt 注入与无效代码。（3）翻译任务服务 translate-service：一次最多 10 种目标语言、p-queue 并发上限 3、按语言记录执行步骤、聚合后一次写回、部分失败保留成功结果；任务列表用源文本摘要作为 productName。共享基础抽到 task-common（MarketingServiceError/resolveModelWithPrecheck/mapUpstreamError），通用状态聚合 aggregateOutcomeStatus 支持任意步骤集合。（4）API 按 module 分派：createMarketingTaskSchema 改为 discriminatedUnion（copywriting/translate），翻译请求走独立校验链路；导出 Markdown 支持多语言结果。（5）翻译 Tab 前端：源文本（10000 字上限）、源语言选择（auto+30 种分组）、LanguagePicker 目标语言多选（分组+搜索+chips+上限 10）、并发提示、结果按语言分组展示+复制+失败状态；sessionStorage 草稿抽为 useSessionDraft hook（useSyncExternalStore）。（6）修复移动端全局布局：Sidebar 改为响应式抽屉（汉堡菜单+遮罩+md:static），修复 390px 视口主区被侧边栏挤压问题。新增 17 个单元测试与 6 个 E2E 用例，累计单元 114、E2E 21 全部通过；tsc/build/lint 通过（修改文件零错误）；实测翻译 API 返回 MODEL_NOT_FOUND 与 VALIDATION_ERROR（非法语言代码）符合契约。
- **受影响文件**：
  - `src/lib/marketing/translate-engine.ts`（新增）
  - `src/lib/marketing/language-codes.ts`（新增）
  - `src/lib/marketing/translate-service.ts`（新增）
  - `src/lib/marketing/task-common.ts`（新增，共享错误与预检）
  - `src/lib/marketing/task-service.ts`（移除重复实现，改用 task-common）
  - `src/lib/marketing/task-status.ts`（新增 aggregateOutcomeStatus 通用聚合）
  - `src/lib/marketing/schemas.ts`（discriminatedUnion + 翻译 Schema 与上限常量）
  - `src/types/marketing-contract.ts`（翻译请求/结果/响应类型）
  - `src/app/api/marketing/tasks/route.ts`（按 module 分派）
  - `src/app/api/marketing/tasks/[id]/export/route.ts`（Markdown 支持翻译结果）
  - `src/components/marketing/workspace/TranslateTab.tsx`（新增）
  - `src/components/marketing/workspace/LanguagePicker.tsx`（新增）
  - `src/components/marketing/workspace/use-session-draft.ts`（新增）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（挂载翻译 Tab）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（本轮作品支持翻译结果）
  - `src/components/shared/Sidebar.tsx`（响应式抽屉）
  - `src/components/shared/Header.tsx`（汉堡菜单按钮）
  - `src/components/shared/DashboardLayout.tsx`（移动端菜单状态）
  - `src/lib/marketing/__tests__/translate-engine.test.ts`（新增）
  - `src/lib/marketing/__tests__/translate-service.test.ts`（新增）
  - `tests/e2e/marketing.spec.ts`（翻译 Tab 交互用例）

## [01:30:00] Phase 4 SEO 与全部作品增强

- **修改说明**：按 V3 文档执行 Phase 4。（1）SEO 引擎 seo-engine：结构化输出（关键词意图/页面标题/标题结构/正文/FAQ/Alt/内链建议/JSON-LD 对象/pendingFacts），全部经 seoResultSchema 严格校验（JSON-LD 必须为对象结构，导出时才序列化）；铁律 prompt 禁止认证/销量/排名/搜索量等未经证实声明进入可发布正文；服务端 findFactViolations 校验违规内容（标题/Meta/正文/FAQ 任一位置），违规时带强化指令修复一次、仍违规则 OUTPUT_INVALID 拦截。（2）事实来源类型扩展为 user/image_analysis/web/model（向后兼容扩展）。（3）SEO 任务服务 seo-service：单次模型调用、执行步骤 seo 键、聚合一次写回；用户输入事实为 confirmed 随 input 快照保存。（4）API 分派增加 seo 分支；导出 Markdown 支持 SEO 结果并序列化 JSON-LD。（5）SEO Tab 前端：页面主题、已有内容（2 万字）、关键词（最多 20）、品类、语言、已确认事实 key-value 添加/删除、内容模型；顶部明确提示"不提供实时搜索量/排名/竞品数据"；结果全量展示（含 JSON-LD 代码预览与待确认事实醒目卡片）。（6）全部作品组合筛选：新增类型（文案/翻译/SEO/GEO/洞察）、时间（最近 7 天/30 天，后端 from/to datetime 校验）筛选，与状态/收藏/搜索组合；列表项显示模块徽章。（7）修复测试竞态：页面渲染断言改为轮询。新增 17 个单元测试与 6 个 E2E 用例，累计单元 131、E2E 27 全部通过；tsc/build/lint 通过（修改文件零错误）；实测 SEO API 校验路径（MODEL_NOT_FOUND 400）符合契约。
- **受影响文件**：
  - `src/lib/marketing/seo-engine.ts`（新增）
  - `src/lib/marketing/seo-service.ts`（新增）
  - `src/lib/marketing/schemas.ts`（SEO 输入/结果 Schema + union 分支）
  - `src/types/marketing-contract.ts`（SEO 类型、事实来源扩展、响应类型）
  - `src/lib/marketing/task-service.ts`（列表时间筛选 from/to）
  - `src/app/api/marketing/tasks/route.ts`（seo 分派、from/to 查询参数）
  - `src/app/api/marketing/tasks/[id]/export/route.ts`（SEO Markdown + JSON-LD 序列化）
  - `src/components/marketing/workspace/SeoTab.tsx`（新增）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（挂载 SEO Tab）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（SEO 结果渲染、组合筛选、模块徽章）
  - `src/lib/marketing/__tests__/seo-engine.test.ts`（新增）
  - `src/lib/marketing/__tests__/seo-service.test.ts`（新增）
  - `src/lib/marketing/__tests__/schemas.test.ts`（重写并新增 SEO Schema 用例）
  - `tests/e2e/marketing.spec.ts`（SEO Tab、组合筛选用例；渲染竞态修复）

## [01:40:00] Phase 5 GEO 离线版

- **修改说明**：按 V3 文档执行 Phase 5。（1）GEO 引擎 geo-engine（离线版）：prompt 铁律禁止联网与训练记忆补充事实、禁止来源列表/引用编号（[1]）/已核实标识/实时性文案（"最新""截至20XX"）；输出经 geoResultSchema strict 校验（结构上不存在 sourceList/verified/citations 字段，模型输出即校验失败）；服务端三重合规校验：findBannedContentViolations（禁止词模式扫描可发布字段）、findGeoFactLeaks（pendingFacts 值泄漏检测）、findClaimKeyViolations（claims.factKey 必须引用输入中已确认事实 key，防编造引用）；违规带强化指令修复一次、仍违规 OUTPUT_INVALID 拦截。（2）GEO 任务服务 geo-service：单次调用、geo 步骤键、聚合一次写回。（3）API 分派增加 geo 分支；导出 Markdown 支持 GEO 结果且首行固定"本结果未联网核实"声明。（4）GEO Tab 前端：目标用户问题（500 字）、品牌/产品名、已有内容、关键词、语言、已确认事实、内容模型；顶部与结果区均固定展示离线声明；结果展示直接回答/支撑内容/事实断言（带 factKey 徽章）/相关追问/待确认事实。（5）抽取 FactInput 共享组件（SEO/GEO 复用，labelPrefix 区分表单控件）。（6）新增 17 个单元测试与 3 个 E2E 用例，累计单元 148、E2E 30 全部通过；tsc/build/lint 通过（修改文件零错误）；实测 GEO API 校验路径（MODEL_NOT_FOUND 400）符合契约。
- **受影响文件**：
  - `src/lib/marketing/geo-engine.ts`（新增）
  - `src/lib/marketing/geo-service.ts`（新增）
  - `src/lib/marketing/schemas.ts`（GEO 输入/结果 Schema + union 分支）
  - `src/types/marketing-contract.ts`（GEO 类型、响应类型）
  - `src/app/api/marketing/tasks/route.ts`（geo 分派）
  - `src/app/api/marketing/tasks/[id]/export/route.ts`（GEO Markdown + 离线声明）
  - `src/components/marketing/workspace/GeoTab.tsx`（新增）
  - `src/components/marketing/workspace/FactInput.tsx`（新增共享组件）
  - `src/components/marketing/workspace/SeoTab.tsx`（改用 FactInput）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（挂载 GEO Tab，离线版标识）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（GEO 结果渲染含离线声明）
  - `src/lib/marketing/__tests__/geo-engine.test.ts`（新增）
  - `src/lib/marketing/__tests__/geo-service.test.ts`（新增）
  - `src/lib/marketing/__tests__/schemas.test.ts`（GEO Schema 用例）
  - `tests/e2e/marketing.spec.ts`（GEO Tab 用例；事实输入标签前缀）

## [02:10:00] Phase 6 持久化异步任务

- **修改说明**：按 V3 文档执行 Phase 6。（1）Prisma 迁移：MarketingTaskItem（kind/role/modelId/dependsOn/status/attempts/maxAttempts/input/result/leaseOwner/leaseExpiresAt）、MarketingTaskEvent（审计事件流）、MarketingTask.cancelRequestedAt、SearchServiceConfig（Phase 7 预留）。（2）异步任务创建 task-creation：四模块统一拆 items（文案 4 项且下游 dependsOn=analysis、翻译每语言一项、SEO/GEO 单项），模型预检保持，立即返回 taskId。（3）Worker（进程内轮询，MARKETING_WORKER_ENABLED 可关）：原子租约领取（updateMany pending+租约空/过期→running）、租约过期恢复（进程重启不丢任务）、失败按 attempts/maxAttempts 回退 pending 重试或终态 failed、依赖分析字段的领取过滤、取消任务（pending 项 cancelled+运行中完成后续聚 cancelled）、单项重试（failed/cancelled→pending，事件 item_retried）、全部终态后聚合一次写回（mergeItemResults 四模块结果合并+兼容字段+事件 task_completed/task_failed/task_cancelled）。（4）API：POST 改异步、详情含 items 进度、POST /:id/cancel、POST /:id/items/:itemId/retry、GET /:id/events（游标）、GET /:id/stream（SSE，心跳+5 分钟超时保护）。（5）前端：useTaskPolling/useAsyncGeneration hooks，四 Tab 提交后轮询，本轮作品显示 items 实时进度+取消按钮+失败项重试按钮，全部作品运行中任务可取消；sessionStorage 记录 taskId 后关页继续执行。（6）真实链路验收：SEO 任务真实模型执行（创建→领取→失败重试→completed 九字段结果）、事件流完整、翻译任务取消→cancelled、失败项重试→重新排队。宽松事实 Schema 归一化修复真实模型 pendingFacts 形态差异。新增 11 个单元测试，累计 159 全部通过；tsc/build/lint 通过；E2E 30 通过。
- **受影响文件**：
  - `prisma/schema.prisma`（TaskItem/Event/cancelRequestedAt/SearchServiceConfig）
  - `prisma/migrations/20260813021505_add_async_items_events_search/`（新增）
  - `src/lib/marketing/async/types.ts`（新增）
  - `src/lib/marketing/async/task-creation.ts`（新增）
  - `src/lib/marketing/async/executors.ts`（新增）
  - `src/lib/marketing/async/aggregation.ts`（新增）
  - `src/lib/marketing/async/worker.ts`（新增）
  - `src/lib/marketing/async/__tests__/aggregation.test.ts`（新增）
  - `src/lib/marketing/async/__tests__/task-creation.test.ts`（新增）
  - `src/lib/marketing/schemas.ts`（looseMarketingFactSchema 宽松事实）
  - `src/lib/marketing/seo-engine.ts`/`geo-engine.ts`（pendingFacts 宽松归一化）
  - `src/app/api/marketing/tasks/route.ts`（异步化）
  - `src/app/api/marketing/tasks/[id]/route.ts`（详情含 items）
  - `src/app/api/marketing/tasks/[id]/cancel/route.ts`（新增）
  - `src/app/api/marketing/tasks/[id]/items/[itemId]/retry/route.ts`（新增）
  - `src/app/api/marketing/tasks/[id]/events/route.ts`（新增）
  - `src/app/api/marketing/tasks/[id]/stream/route.ts`（新增，SSE）
  - `src/components/marketing/workspace/use-task-polling.ts`（新增）
  - `src/components/marketing/workspace/use-async-generation.ts`（新增）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（重写，progress 流转）
  - `src/components/marketing/workspace/{Copywriting,Translate,Seo,Geo}Tab.tsx`（异步提交改造）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（进度视图/取消/重试）

## [02:30:00] Phase 7 联网 GEO 与市场洞察

- **修改说明**：按 V3 文档执行 Phase 7。（1）搜索服务 ADR（docs/adr/0001-search-service.md）落实联网门禁：供应商协议（Tavily 默认/Serper/自定义 OpenAPI）、用户密钥服务端加密保存、每洞察任务最多 12 次查询（可配 1-20）、进程内缓存 TTL 6 小时、URL 去重+同域名最多 3 条、第一版不抓取页面正文、引用展示+检索时间+时效免责、仅发送查询词、失败降级（degraded 标记+无来源时明确提示）。（2）SearchAdapter：HttpSearchAdapter（协议解析/重试一次/缓存去重/脱敏日志）+ MockSearchAdapter + QueryBudget 配额跟踪。（3）SearchServiceConfig（Prisma 表+CRUD API+实测 API：一次真实查询返回含 URL 结果即通过）+ 设置页「搜索服务」分区（新增/实测/删除/配额配置）。（4）联网 GEO：GeoEngine.generateOnline（搜索来源注入+来源编号引用+sources/retrievedAt/degraded 输出+离线铁律沿用），GEO Tab 增加「联网核实」开关与声明切换；任务输入 enableSearch 驱动 executor 分支，无搜索服务 → SEARCH_NOT_CONFIGURED。（5）市场洞察四种（竞品分析/趋势洞察/用户需求分析/价格与定位分析）：InsightEngine 构建类型化查询集→预算内搜索→结果注入→模型生成报告（Zod 校验，外部结论必须标注来源编号、不得编造数据）→sources/degraded/retrievedAt；Insight Tab 未配置状态（按钮禁用+设置跳转）与完整报告展示（摘要/分节/关键发现/建议/来源链接/时效免责）。（6）错误码新增 SEARCH_NOT_CONFIGURED；createMarketingTaskSchema 增加 insight 分支与 geo enableSearch；executor 增加 insight 分支；ResultsPanel 支持洞察结果渲染。实测验证：未配置搜索服务时洞察提交返回 400 SEARCH_NOT_CONFIGURED；搜索服务创建+假 Key 实测 401 失败降级；新增 13 个单元测试，累计 172 全部通过；tsc/build/prisma validate/lint 通过（修复 settings 存量 any 与 effect 模式）；E2E 30 通过。
- **受影响文件**：
  - `docs/adr/0001-search-service.md`（新增）
  - `src/lib/search/SearchAdapter.ts`（新增）
  - `src/lib/search/insight-engine.ts`（新增）
  - `src/lib/search/search-service-config.ts`（新增）
  - `src/lib/search/__tests__/SearchAdapter.test.ts`（新增）
  - `src/lib/search/__tests__/insight-engine.test.ts`（新增）
  - `src/app/api/search-services/route.ts`（新增）
  - `src/app/api/search-services/[id]/route.ts`（新增）
  - `src/app/api/search-services/[id]/test/route.ts`（新增）
  - `src/app/(dashboard)/settings/search-service-section.tsx`（新增）
  - `src/app/(dashboard)/settings/page.tsx`（搜索服务 Tab；修复存量 any/effect）
  - `src/components/marketing/workspace/InsightTab.tsx`（新增）
  - `src/components/marketing/workspace/GeoTab.tsx`（联网核实开关）
  - `src/components/marketing/workspace/MarketingWorkbench.tsx`（挂载洞察 Tab）
  - `src/components/marketing/workspace/ResultsPanel.tsx`（洞察结果渲染）
  - `src/lib/marketing/geo-engine.ts`（generateOnline 联网版）
  - `src/lib/marketing/schemas.ts`（insight/geoOnline Schema + union 分支）
  - `src/lib/marketing/async/task-creation.ts`（insight 分支+门禁校验）
  - `src/lib/marketing/async/executors.ts`（insight/geoOnline 执行分支）
  - `src/types/marketing-contract.ts`（Insight 类型/SEARCH_NOT_CONFIGURED/SearchSource）
  - `tests/e2e/marketing.spec.ts`（市场洞察门禁用例）
